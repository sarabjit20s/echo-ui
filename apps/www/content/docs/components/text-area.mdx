---
title: Text Area
description: A text area that allows the user to enter multiple lines of text.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="TextArea" />

## Installation

<Steps>

<Step>
Copy and paste the following code into `components/ui/TextArea.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('TextArea')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyTextArea.tsx"
import { TextArea } from '@/components/ui/TextArea';

export function MyTextArea() {
  return (
    <TextArea placeholder="Comment" />
  );
}
```