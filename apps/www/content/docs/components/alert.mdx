---
title: Alert
description: Short message to attract user’s attention.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Alert" />

## Installation

<Steps>

<Step>
Make sure you have installed the following dependencies:
#### Internal Dependencies
  - <Link href={'/docs/components/Icon'}>Icon</Link>
  - <Link href={'/docs/components/Text'}>Text</Link>
</Step>

<Step>
Copy and paste the following code into `components/ui/Alert.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Alert')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyAlert.tsx"
import {
  Alert,
  AlertDescription,
  AlertIcon,
  AlertTitle,
} from '@/components/ui/Alert';

export function MyAlert() {
  return (
    <Alert>
      <AlertIcon name="time" />
      <AlertTitle>No days available</AlertTitle>
      <AlertDescription>
        Days will appear here when more photos and videos are added to the library.
      </AlertDescription>
    </Alert>
  );
}
```