---
title: Text
description: Display text using well-defined typographic styles.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Text" />

## Installation

<Steps>

<Step>
Copy and paste the following code into `components/ui/Text.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Text')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyText.tsx"
import { Text } from '@/components/ui/Text';

export function MyText() {
  return (
    <Text>Typography is a set of rules that determine the appearance of text.</Text>
  );
}
```