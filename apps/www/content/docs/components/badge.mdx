---
title: Badge
description: A Badge is a small visual indicator used to display a short piece of information, such as a number, label, or status.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Badge" />

## Installation

<Steps>

<Step>
Make sure you have installed the following dependencies:
#### Internal Dependencies
  - <Link href={'/docs/components/icon'}>Icon</Link>
  - <Link href={'/docs/components/text'}>Text</Link>
</Step>

<Step>
Copy and paste the following code into `components/ui/Badge.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Badge')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyBadge.tsx"
import { Badge, BadgeText } from '@/components/ui/Badge';

export function MyBadge() {
  return (
    <Badge>
      <BadgeText>New</BadgeText>
    </Badge>
  );
}
```