---
title: Avatar
description: Profile picture, user initials or fallback icon.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Avatar" />

## Installation

<Steps>

<Step>
Copy and paste the following code into `components/ui/Avatar.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Avatar')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyAvatar.tsx"
import {
  Avatar,
  AvatarFallback,
  AvatarImage,
  AvatarProps,
  AvatarText,
} from '@/components/ui/Avatar';
import { Text } from '@/components/ui/Text';

export function MyAvatar() {
  return (
    <Avatar>
      <AvatarImage src="https://avatar.iran.liara.run/public?username=John+Doe" />
      <AvatarFallback>
        <AvatarText>
          JD
        </AvatarText>
      </AvatarFallback>
    </Avatar>
  );
}
```