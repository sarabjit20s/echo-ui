---
title: Button
description: Displays a button or a component that looks like a button.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Button" />

## Installation

<Steps>

<Step>
Make sure you have installed the following dependencies:
#### Internal Dependencies
  - <Link href={'/docs/components/icon'}>Icon</Link>
  - <Link href={'/docs/components/text'}>Text</Link>
</Step>

<Step>
Copy and paste the following code into `components/ui/Button.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Button')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyButton.tsx"
import { Button, ButtonText } from '@/components/ui/Button';

export function MyButton() {
  return (
    <Button>
      <ButtonText>Get Started</ButtonText>
    </Button>
  );
}
```