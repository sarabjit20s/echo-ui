---
title: Spinner
description: Displays an animated loading indicator.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Spinner" />

## Installation

<Steps>

<Step>
Make sure you have installed the following dependencies:
#### External Dependencies
  - [React Native Svg](https://github.com/software-mansion/react-native-svg)
</Step>

<Step>
Copy and paste the following code into `components/ui/Spinner.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Spinner')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MySpinner.tsx"
import { Spinner } from '@/components/ui/Spinner';

export function MySpinner() {
  return (
    <Spinner />
  );
}
```