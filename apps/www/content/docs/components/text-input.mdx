---
title: TextInput
description: A text input that allows the user to enter text.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { readComponentFile } from '@/lib/readFile';

## Installation

<Steps>

<Step>
Make sure you have installed the following dependencies:
#### Internal Dependencies
  - <Link href={'/docs/utils/slot'}>Slot</Link>
</Step>

<Step>
Copy and paste the following code into `components/ui/TextInput.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('TextInput')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MyTextInput.tsx"
import { TextInput } from '@/components/ui/TextInput';

export function MyTextInput() {
  return (
    <TextInput />
  );
}
```