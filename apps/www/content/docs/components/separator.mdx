---
title: Separator
description: Visually or semantically separates content.
---

import Link from 'next/link';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { ComponentPreview } from '@/components/component-preview';
import { readComponentFile } from '@/lib/readFile';

## Preview
<ComponentPreview name="Separator" />

## Installation

<Steps>

<Step>
Copy and paste the following code into `components/ui/Separator.tsx`.
<DynamicCodeBlock lang="tsx" code={readComponentFile('Separator')} />
</Step>

<Step>
Update the import paths to match your project setup.
</Step>

</Steps>

## Usage

```tsx title="MySeparator.tsx"
import { Separator } from '@/components/ui/Separator';

export function MySeparator() {
  return (
    <Separator />
  );
}
```